namespace stdx
{

class bar;
private class foo;

}

lookup(bar, "");
lookup(foo, "");

using namespace stdx;

lookup(bar, "stdx.bar");
lookup(foo, "private stdx.foo");

class foo
{
}

lookup(foo, "private stdx.foo, foo");
lookup(.foo, "foo");
