include("stdtest.ph");
include("stdlib.ph");
include("stdios.ph");

func test.assert(b:std.bool, info:std.source_info)
{
    if(!b)
    {
        std.out() << "unittest failed: line " << info.line() << "\n";

        std.abort();
    }
}
